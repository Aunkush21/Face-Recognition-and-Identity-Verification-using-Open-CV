# All are installed in Python 3.10 

import cmake
import dlib #(If pip didn't work download it manually from google)
import cv2
import face_recognition #(Make sure to download the dlib library first)
import pyautogui

print(cmake.__version__)
print(cv2.__version__)
print(dlib.__version__)
print(face_recognition.__version__)
print(pyautogui.__version__)